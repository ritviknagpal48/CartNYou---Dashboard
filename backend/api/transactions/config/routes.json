{
  "routes": [
    {
      "method": "GET",
      "path": "/transactions",
      "handler": "transactions.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/transactions",
      "handler": "transactions.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/transactions",
      "handler": "transactions.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
